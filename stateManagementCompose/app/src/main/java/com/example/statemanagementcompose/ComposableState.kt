package com.example.statemanagementcompose

import androidx.compose.material3.Text
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.ui.unit.sp

/*val sayac = remember {
    mutableStateOf(0)
}

Text(text = "Sayaç : ${sayac.value}")
Button(onClick ={
    sayac.value = sayac.value + 1
}) {
    Text(text = "Tıkla", fontSize = 20.sp)
}





var myString = "mercan"
TextField(value = TextFieldValue(text = myString), onValueChange = {
    myString = it.text
})

Spacer(modifier = Modifier.padding(7.dp))

var textString = "hello"

Text(text = textString, fontSize = 26.sp)

Text(text = "Hello!",
    fontSize = 26.sp)

Spacer(modifier = Modifier.padding(7.dp))

Button(onClick = {
    textString = "android"
    println(textString)



}) {
    Text(text = "Button")
    Text(text = "test")
}
Spacer(modifier = Modifier.padding(7.dp))

//Image(bitmap = ImageBitmap.imageResource(id = R.drawable.ire), contentDescription = null)


Image(imageVector = ImageVector.vectorResource(id = R.drawable.ic_launcher_foreground), contentDescription = null)
 */